# Local demo data

These tiny fixtures provide a repeatable, private-network-free starting point for a screenshot or a manual UI demo. Copy the files into a local `data/` directory before starting the server if you want preloaded incident cards.

The three targets intentionally use loopback addresses. They are demo endpoints, not benchmark destinations.
